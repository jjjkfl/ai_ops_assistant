# 🤖 AI Operations Assistant

A multi-agent AI assistant that accepts natural language tasks, plans execution steps, calls real APIs, and returns structured results.

## 🚀 Quick Start

### 1. Installation
```bash
# Clone and navigate
git clone <repository>
cd ai_ops_assistant

# Create virtual environment
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# Install dependencies
pip install -r requirements.txt
